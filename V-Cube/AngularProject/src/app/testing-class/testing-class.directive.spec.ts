import { TestingClassDirective } from '../testing-class.directive';

describe('TestingClassDirective', () => {
  it('should create an instance', () => {
    const directive = new TestingClassDirective();
    expect(directive).toBeTruthy();
  });
});
